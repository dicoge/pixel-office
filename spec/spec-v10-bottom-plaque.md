# v10 — 底部牌匾 + 咖啡機最終調整

## 背景
用戶回報畫面底部「有兩塊牌子」，只需要底下那塊有星星的版本。咖啡機位置也要微調。

## 需求

### 1. 底部牌匾調整
- 保留 ONE 個底部牌匾（含 ⭐ 星星裝飾）
- 移除其他造成「兩塊」的元素：
  - 檢查 `#status-text`（左下狀態文字）是否多餘，可隱藏或淡化
  - 檢查 vignette 底部橫條 (`fillRect(0, 705, 1280, 15)`) 是否造成視覺混淆
- 牌匾樣式仿 Star Office UI：
  - 深色背景，金色邊框
  - 左右各一顆 ⭐
  - 中央 "PIXEL OFFICE" 金色文字
  - 位置靠近底部但不明顯遮擋畫面

### 2. 咖啡機位置
- x=225（書櫃與桌子之間，檯燈右方）
- y=210（再往上一些）

### 3. 上方招牌
- 保留 x=660（已置中偏右）
- 不動

## 檔案
- `public/game.js` 的 `drawRoom()` 函式
- `public/index.html` 的 `#status-text` 樣式（如需隱藏）

## 注意
- 不要刪掉其他家具/角色
- Phaser Canvas 內繪製，depth 控制圖層
- 完成後 push 到 master 觸發 Render 部署
